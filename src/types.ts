/**
 * User interface
 * @description User interface that contains all the information about a user
 */
export type User = {
  _id: string;
  name: string;
  city: string;
  email: string;
  country: string;
  addressLine1: string;
};
